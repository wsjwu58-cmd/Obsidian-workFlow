---
created: 2026-08-09
updated: 2026-08-09
type: workflow
status: 已验证（服务器 codex 执行引擎）
product: null
source: references/ 分拣规则
---

# 服务器 codex 执行引擎（worker）约数

> 运行器：服务器 Ubuntu（codex-cli），由 GitHub Actions「调度中枢」SSH 触发。
> 行为约束：只处理传入的待处理条目，遵循 references/ 的「索引 → 产出分流」。

## 对每一条待处理做 4 步

1. **抓取原文**：用 `webfetch`/工具抓取该条目 URL 的真实内容（不依赖本地素材，原始素材不存仓库）。
2. **判值**：是否有实质技术内容 + 原创洞察 + 可信来源；不达标 → 在 `references/articles.md`
   该行标 `已淘汰`、不生成任何条目（保留 URL 防重复采集）。
3. **判定归属**（只看这三个落点）：
   | 归属 | 落点 | 何时 |
   |------|------|------|
   | **独立思考** | `expand/thinking/*.md` | 有独立观点/质疑/延伸（默认优先） |
   | **作品** | `working/*.md`（译文/教程/工具模板） | 能产出可独立理解的作品 |
   | **提示词** | `prompts/*.md` | 提炼出可复用的 prompt |
4. **回写 articles.md**：把该条目从「待处理队列」移除，在「已收录」编号正文追加一个
   `### N.` 条目，含：标题 / 链接 / 作者 / 日期 / `状态：已收录` / `归属：expand/thinking/xxx.md 或 working/xxx.md` / 核心；

生成条目的 frontmatter：
```yaml
---
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources: [references/articles.md#N]
tags: [内容标签, type/想法]
---
```

同步义务：新增 `expand/` 条目 → `expand/index.md` 加行 + 计数 +1；`expand/log.md` 加变更记录；
`expand/知识图谱.md` 补关系。**wiki/ 只读，绝不写入。**

## 人话与质量

- 表达独立思考而非转述；无增量不硬写（宁可标记已淘汰）
- 中文正文，代码给最小可运行示例（版本锁定、异常捕获）
- 保持索引 K1 状态机合法（状态 ∈ {待处理, 已收录, 已淘汰}）

## 本次待处理条目

### 待处理 1
标题：MCP 官方文档：Model Context Protocol 介绍
URL：https://modelcontextprotocol.io/introduction
来源：e2e-test | 日期：2026-08-09

## 输出要求
严格按注解 machine-readable 要求回写 references/articles.md （状态→已收录/已淘汰、归属→生成的 expand/thinking 或 working 路径），完成后告知变更摘要。